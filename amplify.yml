version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build step required for static site"
    build:
      commands:
        - echo "Building static site..."
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
